1. Create Vite React JavaScript Project
2. Install and configure Tailwind

My Thinking Process
ดูแบบในฟิกม่าแล้วลองถอดออกมาเป็นชิ้นส่วนเพื่อสร้าง component โดยในเบื้องต้นมีปุ่มและตารางที่ใช้ในหน้า user กับ admin โดยปุ่มสามารถสร้างแบบเดียวแล้วใช้รูปแบบการตกแต่งร่วมกันได้สำหรับปุ่มที่ใช้สลับหน้าและสร้างรูปแบบสำหรับปุ่มลบอีกแบบหนึ่ง ส่วนตารางสร้างแยกกันเพราะมีจุดที่แตกต่างคือแถวที่ใช้ในการลบข้อมูล และยังมีส่วน input ที่สามารถทำแยกเป็นช่องๆใช้ร่วมกันได้ สุดท้ายคือไฟล์จัดวางเลย์เอาท์และไฟล์สำหรับเตรียม navbar หลังจากนั้นจึงเริ่มนำชิ้นส่วนมาประกอบเป็นไฟล์หน้าต่างๆ อย่างหน้าหลักที่มีแค่หัวข้อกับปุ่มและเมื่อกดปุ่มถึงจะเปลี่ยนสลับไปมา และหน้าข้อมูลเดี่ยวที่มีหัวข้อ ภาพ และเนื้อหาบรรยายที่ตอนนี้ใส่เป็นโลเร็มเพราะขี้เกียจคิดค่ะ555555